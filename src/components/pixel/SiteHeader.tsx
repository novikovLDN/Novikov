/**
 * Шапка сайта — переадресация на бренд-слой 2027.
 *
 * Страницы старой системы импортируют этот модуль по всему проекту.
 * Вместо того чтобы править импорт в семнадцати файлах, модуль
 * отдаёт новую шапку: у сайта одна шапка, и она уже переписана.
 *
 * Прежняя реализация (логотип «atlas.secure», пилюли-чипы, пункт
 * «Бизнес») удалена вместе с системой PIXEL: ребрендинг сделан под
 * частных лиц, и корпоративного входа в навигации больше нет.
 *
 * Файл удаляется, когда последняя страница перейдёт на .b-* и станет
 * импортировать BrandHeader напрямую.
 */
export { default } from "@/components/brand/BrandHeader";
